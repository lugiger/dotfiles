# Skills

Personal Claude Code skills, symlinked to `~/.claude/skills`.

Each skill is a directory containing a `SKILL.md` (frontmatter with `name` and
`description`, plus instructions) and any supporting files. See
https://docs.claude.com/en/docs/claude-code/skills for the format.
